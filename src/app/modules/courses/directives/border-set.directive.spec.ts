import { BorderSetDirective } from './border-set.directive';

describe('BorderSetDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderSetDirective();
    expect(directive).toBeTruthy();
  });
});
